<h1>Main Page</h1>
<% @post.each do |post| %>
  <div class="alert alert-light">
    <h3><%= post.title %></h3>
    <p> <%= post.body %> </p>
    <%= link_to "Details", post_path(post), :class => 'btn
    btn-outline-secondary' %>
  </div>

<% end %>
